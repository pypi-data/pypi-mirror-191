from setuptools import setup

setup(name = 'calculatorGS', 
      version = '0.1',
      description = 'A basic calculator, which can add, subtract, multiply, divide and take nth root of a number.',
      author = 'Gediminas Sumskis',
      packages = ['calculatorGS'],
      zip_safe = False)